#include <bits/stdc++.h>
using namespace std;

int main()
{

    int x;
    cin >> x;

    cin.ignore();               // ignoring enter

    string a;
    getline(cin, a);            // input with space

    cout << x << endl;
    cout << a << endl;

    return 0;
}